//! bei → lightyear input integration. Stage N+ wires
//! `lightyear_inputs_bei` so action snapshots flow over the wire as
//! tick-tagged inputs. Today this is a placeholder.

use bevy::prelude::*;

pub struct NetInputPlugin;

impl Plugin for NetInputPlugin {
    fn build(&self, _app: &mut App) {
        // Stage N+: register bei action inputs to flow over the wire.
    }
}
